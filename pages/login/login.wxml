<!--pages/login/login.wxml-->
<view class="login container">
  <view class="background">
   我是图
  </view>
  <view class="buttonArea">
    <button class="loginButton" bindtap="loginClicked">授权登录</button>
  </view>
  let button = wx.createUserInfoButton({
      type: 'text',
      text: '获取用户信息',
      style: {
        left: 10,
        top: 76,
        width: 200,
        height: 40,
        lineHeight: 40,
        backgroundColor: '#ff0000',
        color: '#ffffff',
        textAlign: 'center',
        fontSize: 16,
        borderRadius: 4
      }
    })
    button.onTap((res) => {
      console.log(res)
    })
</view>
